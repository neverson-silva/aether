services:
  evo-auth:
    image: evoapicloud/evo-auth-service-community@sha256:dd5ada499dce64296539baea7abf34ffab41b64ca9e7072112e8673a12a109c8
    command: sh -c "bundle exec rails db:prepare && bundle exec rails s -p 3001 -b 0.0.0.0"
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:3001/up"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s        
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - RAILS_ENV=production
      - RAILS_MAX_THREADS=5
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - EVOAI_CRM_API_TOKEN=${EVOAI_CRM_API_TOKEN}
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_USERNAME=postgres
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DATABASE=evo_crm_production
      - POSTGRES_SSLMODE=disable
      - REDIS_URL=redis://redis:6379/3
      - FRONTEND_URL=${FRONT_URL}
      - BACKEND_URL=${BACK_URL}
      - CORS_ORIGINS=${FRONT_URL},${BACK_URL}
      - DOORKEEPER_JWT_SECRET_KEY=${DOORKEEPER_JWT_SECRET_KEY}
      - DOORKEEPER_JWT_ALGORITHM=${DOORKEEPER_JWT_ALGORITHM}
      - DOORKEEPER_JWT_ISS=${DOORKEEPER_JWT_ISS}
      - MFA_ISSUER=${MFA_ISSUER}

  evo-auth-sidekiq:
    image: evoapicloud/evo-auth-service-community@sha256:dd5ada499dce64296539baea7abf34ffab41b64ca9e7072112e8673a12a109c8
    command: ["bundle", "exec", "sidekiq", "-C", "config/sidekiq.yml"]
    restart: always
    healthcheck:
      disable: true    
    depends_on:
      evo-auth:
        condition: service_healthy        
    environment:
      - RAILS_ENV=production
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - EVOAI_CRM_API_TOKEN=${EVOAI_CRM_API_TOKEN}
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_USERNAME=postgres
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DATABASE=evo_crm_production
      - POSTGRES_SSLMODE=disable
      - REDIS_URL=redis://redis:6379/3
      - DOORKEEPER_JWT_SECRET_KEY=${DOORKEEPER_JWT_SECRET_KEY}
      - DOORKEEPER_JWT_ALGORITHM=${DOORKEEPER_JWT_ALGORITHM}
      - DOORKEEPER_JWT_ISS=${DOORKEEPER_JWT_ISS}
      - MFA_ISSUER=EvoCRM
      - SIDEKIQ_CONCURRENCY=10

  evo-crm:
    image: evoapicloud/evo-ai-crm-community@sha256:fd284bba566ae08a4f18bd7fda8c0b16e3d092cf81f23b62d179c1375d640e4f
    command: sh -c "bundle exec rails db:prepare && bundle exec rails s -p 3000 -b 0.0.0.0"
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://127.0.0.1:3000/health/live"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s    
    depends_on:
      evo-auth:
        condition: service_healthy        
    environment:
      - RAILS_ENV=production
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - EVOAI_CRM_API_TOKEN=${EVOAI_CRM_API_TOKEN}
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_USERNAME=postgres
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DATABASE=evo_crm_production
      - POSTGRES_SSLMODE=disable
      - REDIS_URL=redis://redis:6379/3
      - EVO_AUTH_SERVICE_URL=http://evo-auth:3001
      - EVO_AI_CORE_SERVICE_URL=http://evo-core:5555
      - BACKEND_URL=${BACK_URL}
      - FRONTEND_URL=${FRONT_URL}
      - CORS_ORIGINS=${FRONT_URL},${BACK_URL}
      - EVOLUTION_API_ONLY_SERVER=true
      - DISABLE_TELEMETRY=true
      - LOG_LEVEL=info
      - ENABLE_ACCOUNT_SIGNUP=true
      - ENABLE_PUSH_RELAY_SERVER=true
      - ENABLE_INBOX_EVENTS=true
      - BOT_RUNTIME_URL=http://evo-bot-runtime:8080
      - BOT_RUNTIME_SECRET=${BOT_RUNTIME_SECRET}
      - BOT_RUNTIME_POSTBACK_BASE_URL=${BACK_URL}
    volumes:
      - evo-crm-data:/app/storage

  evo-crm-sidekiq:
    image: evoapicloud/evo-ai-crm-community@sha256:fd284bba566ae08a4f18bd7fda8c0b16e3d092cf81f23b62d179c1375d640e4f
    command: ["bundle", "exec", "sidekiq", "-C", "config/sidekiq.yml"]
    healthcheck:
      disable: true       
    depends_on: 
      evo-crm:
        condition: service_healthy     
    environment:
      - RAILS_ENV=production
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - EVOAI_CRM_API_TOKEN=${EVOAI_CRM_API_TOKEN}
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_USERNAME=postgres
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DATABASE=evo_crm_production
      - POSTGRES_SSLMODE=disable
      - REDIS_URL=redis://redis:6379/3
      - EVO_AUTH_SERVICE_URL=${AUTH_URL}
      - EVO_AI_CORE_SERVICE_URL=http://evo-core:5555
      - BACKEND_URL=${BACK_URL}
      - FRONTEND_URL=${FRONT_URL}
      - CORS_ORIGINS=${FRONT_URL},${BACK_URL},${AUTH_URL}
      - DISABLE_TELEMETRY=true
      - LOG_LEVEL=info
      - ENABLE_ACCOUNT_SIGNUP=true
      - ENABLE_PUSH_RELAY_SERVER=true
      - ENABLE_INBOX_EVENTS=true
      - BOT_RUNTIME_URL=http://evo-bot-runtime:8080
      - BOT_RUNTIME_SECRET=${BOT_RUNTIME_SECRET}
      - BOT_RUNTIME_POSTBACK_BASE_URL=${BACK_URL}
      - SIDEKIQ_CONCURRENCY=10

  evo-core:
    image: evoapicloud/evo-ai-core-service-community@sha256:371d826b8d5117c9c554fe3f487750b633cd68ca491615fd592c6dac6ca260d3
    restart: always
    depends_on:
      postgres:
        condition: service_healthy    
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=${POSTGRES_PASSWORD}
      - DB_NAME=evo_crm_production
      - DB_SSLMODE=disable
      - DB_MAX_IDLE_CONNS=10
      - DB_MAX_OPEN_CONNS=100
      - DB_CONN_MAX_LIFETIME=1h
      - DB_CONN_MAX_IDLE_TIME=30m
      - PORT=5555
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - JWT_ALGORITHM=HS256
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - EVOLUTION_BASE_URL=http://evo-crm:3000
      - EVO_AUTH_BASE_URL=http://evo-auth:3001
      - AI_PROCESSOR_URL=http://evo-processor:8000
      - AI_PROCESSOR_VERSION=v1

  evo-processor:
    image: evoapicloud/evo-ai-processor-community@sha256:1a97c7bc033658b7dcaacdd0ef10023521490b3a5c80870f60d9a9e7ed09521c
    depends_on:
      evo-crm:
        condition: service_healthy       
    environment:
      - POSTGRES_CONNECTION_STRING=postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/evo_crm_production
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_SSL=false
      - REDIS_DB=3
      - REDIS_KEY_PREFIX="a2a:"
      - REDIS_TTL=3600
      - HOST=0.0.0.0
      - PORT=8000
      - DEBUG=false
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - EVOAI_CRM_API_TOKEN=${EVOAI_CRM_API_TOKEN}
      - EVO_AI_CRM_URL=http://evo-crm:3000
      - CORE_SERVICE_URL=http://evo-core:5555/api/v1
      - EVO_AUTH_BASE_URL=http://evo-auth:3001
      - APP_URL=${FRONT_URL}
      - API_URL=${PROCESSOR_URL}
      - API_TITLE=Agent Processor Community
      - API_DESCRIPTION=Agent Processor Community for Evolution AI
      - API_VERSION=1.0.0  
      - ORGANIZATION_NAME=Evolution CRM  
      - TOOLS_CACHE_ENABLED=true
      - TOOLS_CACHE_TTL=3600

  evo-bot-runtime:
    image: evoapicloud/evo-bot-runtime@sha256:f6888bfe2cae42191c74f88b92dbf61a3ae20d94f5d2d35e26628f7527cc4139
    environment:
      - LISTEN_ADDR=0.0.0.0:8080
      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/3
      - AI_PROCESSOR_URL=http://evo-processor:8000
      - BOT_RUNTIME_SECRET=${BOT_RUNTIME_SECRET}
      - AI_CALL_TIMEOUT_SECONDS=30

  evo-crm-frontend:
    image: evoapicloud/evo-ai-frontend-community@sha256:dc1f5b6dae1640cf2630010ddec04daa2d20bf269536ef82c58852c698b7808c
    environment:
      - VITE_APP_ENV=production
      - VITE_API_URL=${BACK_URL}
      - VITE_AUTH_API_URL=${AUTH_URL}
      - VITE_EVOAI_API_URL=${EVOIA_URL}
      - VITE_AGENT_PROCESSOR_URL=${PROCESSOR_URL}
      - VITE_WS_URL=wss://${BACK_DOMAIN}
      - VITE_FRONT_URL=${FRONT_URL}

  postgres:
    image: postgres:16-alpine
    restart: always
    volumes:
      - postgres-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=${POSTGRES_DATABASE}
      - POSTGRES_USER=${POSTGRES_USERNAME}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
                
  redis:
    image: redis:alpine
    restart: always
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
  redis-data:
  evo-processor-data:
  evo-crm-data:
