// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: gitops.sql

package pg

import (
	"context"

	"github.com/google/uuid"
)

const createGitOps = `-- name: CreateGitOps :one
INSERT INTO gitops (org_id, name, repo_url, branch, path, apply_mode)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, org_id, name, repo_url, branch, path, target_org_id, apply_mode, last_sha, last_status, drift_added, drift_changed, drift_removed, last_sync, created_at
`

type CreateGitOpsParams struct {
	OrgID     uuid.UUID `json:"org_id"`
	Name      string    `json:"name"`
	RepoUrl   string    `json:"repo_url"`
	Branch    string    `json:"branch"`
	Path      string    `json:"path"`
	ApplyMode string    `json:"apply_mode"`
}

func (q *Queries) CreateGitOps(ctx context.Context, arg CreateGitOpsParams) (Gitop, error) {
	row := q.db.QueryRowContext(ctx, createGitOps,
		arg.OrgID,
		arg.Name,
		arg.RepoUrl,
		arg.Branch,
		arg.Path,
		arg.ApplyMode,
	)
	var i Gitop
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.Name,
		&i.RepoUrl,
		&i.Branch,
		&i.Path,
		&i.TargetOrgID,
		&i.ApplyMode,
		&i.LastSha,
		&i.LastStatus,
		&i.DriftAdded,
		&i.DriftChanged,
		&i.DriftRemoved,
		&i.LastSync,
		&i.CreatedAt,
	)
	return i, err
}

const deleteGitOps = `-- name: DeleteGitOps :exec
DELETE FROM gitops
WHERE id = $1 AND org_id = $2
`

type DeleteGitOpsParams struct {
	ID    uuid.UUID `json:"id"`
	OrgID uuid.UUID `json:"org_id"`
}

func (q *Queries) DeleteGitOps(ctx context.Context, arg DeleteGitOpsParams) error {
	_, err := q.db.ExecContext(ctx, deleteGitOps, arg.ID, arg.OrgID)
	return err
}

const getGitOps = `-- name: GetGitOps :one
SELECT id, org_id, name, repo_url, branch, path, target_org_id, apply_mode, last_sha, last_status, drift_added, drift_changed, drift_removed, last_sync, created_at
FROM gitops
WHERE id = $1
`

func (q *Queries) GetGitOps(ctx context.Context, id uuid.UUID) (Gitop, error) {
	row := q.db.QueryRowContext(ctx, getGitOps, id)
	var i Gitop
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.Name,
		&i.RepoUrl,
		&i.Branch,
		&i.Path,
		&i.TargetOrgID,
		&i.ApplyMode,
		&i.LastSha,
		&i.LastStatus,
		&i.DriftAdded,
		&i.DriftChanged,
		&i.DriftRemoved,
		&i.LastSync,
		&i.CreatedAt,
	)
	return i, err
}

const listGitOpsByOrg = `-- name: ListGitOpsByOrg :many
SELECT id, org_id, name, repo_url, branch, path, target_org_id, apply_mode, last_sha, last_status, drift_added, drift_changed, drift_removed, last_sync, created_at
FROM gitops
WHERE org_id = $1
ORDER BY name
`

func (q *Queries) ListGitOpsByOrg(ctx context.Context, orgID uuid.UUID) ([]Gitop, error) {
	rows, err := q.db.QueryContext(ctx, listGitOpsByOrg, orgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Gitop{}
	for rows.Next() {
		var i Gitop
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.Name,
			&i.RepoUrl,
			&i.Branch,
			&i.Path,
			&i.TargetOrgID,
			&i.ApplyMode,
			&i.LastSha,
			&i.LastStatus,
			&i.DriftAdded,
			&i.DriftChanged,
			&i.DriftRemoved,
			&i.LastSync,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateGitOpsSync = `-- name: UpdateGitOpsSync :exec
UPDATE gitops
SET last_sha = $2,
    last_status = $3,
    drift_added = $4,
    drift_changed = $5,
    drift_removed = $6,
    last_sync = now()
WHERE id = $1
`

type UpdateGitOpsSyncParams struct {
	ID           uuid.UUID `json:"id"`
	LastSha      string    `json:"last_sha"`
	LastStatus   string    `json:"last_status"`
	DriftAdded   int32     `json:"drift_added"`
	DriftChanged int32     `json:"drift_changed"`
	DriftRemoved int32     `json:"drift_removed"`
}

func (q *Queries) UpdateGitOpsSync(ctx context.Context, arg UpdateGitOpsSyncParams) error {
	_, err := q.db.ExecContext(ctx, updateGitOpsSync,
		arg.ID,
		arg.LastSha,
		arg.LastStatus,
		arg.DriftAdded,
		arg.DriftChanged,
		arg.DriftRemoved,
	)
	return err
}
