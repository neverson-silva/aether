root = "."
[build]
  cmd = "go build -o /tmp/aether-dev ./cmd/api"
  bin = "/tmp/aether-dev"
  include_ext = ["go"]
  include_dir = ["cmd", "internal"]
  delay = 500
  stop_on_error = true
[log]
  time = true
